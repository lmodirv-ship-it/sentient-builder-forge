# Roadmap — تنفيذ خطة لوحة التحكم والنواة الذكية

## 1) الأساس — الأدوار + حماية المالك + profiles + بوابة /admin
- [ ] Migration: `app_role`, `user_roles`, `has_role`, `is_staff`, `claim_ownership`, مُشغِّل حماية المالك
- [ ] Migration: `profiles` + باقي الجداول مع GRANT + RLS
- [ ] استدعاء `claim_ownership` عند الدخول (lmodirv@gmail.com)
- [ ] دوال خادم لدور المستخدم + حارس `/admin`

## 2) الإدارة — جداول الخدمات والإعدادات والسجلات + شاشات
- [ ] شاشات: نظرة عامة، المستخدمون، خدمات HN، الإعدادات، السجلات، القوالب، النواة

## 3) التتبّع — ربط دوال الإنشاء بـ `generation_jobs` + `activity_logs`

## 4) الذاكرة — `knowledge_items` + `feedback_events` + أزرار التقييم في الاستوديو

## 5) التعلّم — بحث دلالي + `model_policies` + وظيفة تحديث دورية

## 6) التحسين — الحدود والحصص + لوحة إحصاءات + إعادة استخدام النتائج
