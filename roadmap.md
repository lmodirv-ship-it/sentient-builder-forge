# خريطة تنفيذ خطة منصة النواة

## مكتمل
- [x] المرحلة 1 — الأساس: ترحيل الأدوار (owner/admin/editor/user) + حماية المالك + جداول المنصة (GRANT+RLS) + بيانات أولية
- [x] المرحلة 2 — الإدارة: دوال /admin + صفحة لوحة التحكم (نظرة عامة، مستخدمون، خدمات HN، إعدادات، سجلات، قوالب، نواة)
- [x] تتبّع كل عمليات الإنشاء في generation_jobs + activity_logs + فحص الحصص اليومية
- [x] أزرار التقييم (👍/👎/إعادة) في استوديوهات الصور/الفيديو/المواقع/السيرة → feedback_events + knowledge_items
- [x] نقطة /api/public/cron-learn للتعلّم الدوري + إعادة تدريب يدوية وتنظيف الذاكرة في اللوحة

## متبقٍ
- [ ] المرحلة 4 — بحث دلالي بالتضمينات (Embeddings عبر HN) بدل التقاطع اللفظي في kernelSuggest
- [ ] المرحلة 5 — جدولة cron-learn خارجياً (pg_cron) مع NAWAT_CRON_SECRET
- [ ] المرحلة 6 — لوحة إحصاءات زمنية (رسوم بيانية) وتنبيهات فشل الخدمات

## خطة خادم VPS 66 (معتمدة)
- [x] كود المحرك المركزي في `engine/` — الوكيل، سجل الأدوات، طبقة التشغيل الموحّدة، الذاكرة، طابور المهام، القوالب، نقاط `/v1`
- [x] ملفات النشر: `engine/deploy/nginx.conf` + `nawat-engine.service` + `engine/README.md` + `engine/schema.sql`
- [x] عميل الواجهة `src/lib/engine-client.ts` ومسار الدردشة يستخدم المحرك عند ضبط `VITE_ENGINE_URL`
- [ ] تثبيت المحرك على `213.156.132.166` (يحتاج دخول الخادم)
- [ ] توجيه `api.hn-chat.com` إلى الخادم وإصدار الشهادة (يحتاج تأكيدك)
- [ ] نقل بيانات القوالب والسجلات إلى قاعدة الخادم
- [ ] ربط تطبيق الهاتف وبناء APK بعد استقرار الخادم

## خادم 213.156.133.94 (نسخة التطوير)
- [x] سوبرابيز الذاتي: مسار /sb على hn-ai.pro + نقل قاعدة nawat (111 قالباً) + إنشاء حساب المالك
- [x] بناء الواجهة بصيغة خادم Node ونشرها في /opt/nawat-web كخدمة nawat-web (منفذ 4310)
- [x] تجربة كاملة: دخول المالك وسؤال النواة يعمل من قاعدة الخادم
- [ ] ربط اسم النطاق (ينتظر اسم النطاق من المستخدم) ثم إصدار الشهادة وفتح المنفذ في nginx

## ربط النواة بمنصة TVCC
- [x] إضافة محتوى دليل مجموعة مواقع HN (TVCC_Guide_AR_3.pdf) إلى ذاكرة النواة على الخادم (120 مقطعاً، source=tvcc-guide)
- [x] عميل TVCC في `src/lib/tvcc.functions.ts` وربطه بمسار الدردشة
- [ ] إرسال رسائل من النواة إلى TVCC لنشرها في باقي المواقع (يحتاج نقطة نشر في TVCC)
